# Window Manager Playground

This plugin loads `@blcklab/freedom@0.2.1/dist/manager.js` and runs the real `createManager()` implementation. Lightweight window doubles expose the same manager-facing methods, making focus, registration, events, and z-index examples fully executable in Scriptoria's Worker.
